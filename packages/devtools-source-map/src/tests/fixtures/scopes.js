// generated with:
// ../../../node_modules/uglify-es/bin/uglifyjs scopes.js -m --source-map -o "scopes.min.js"

function scopes() {
    let zero;

    {
        let one = 1;
        one++;
    }

    zero = 0;
}